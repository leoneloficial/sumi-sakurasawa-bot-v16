<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - SubBot Manager</title>
    <style>
        body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; background: #f0f2f5; padding: 2rem; margin: 0; }
        .header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 2rem; }
        .card { background: white; padding: 2rem; border-radius: 8px; box-shadow: 0 4px 6px rgba(0,0,0,0.1); max-width: 600px; margin: 0 auto; }
        h1, h2 { color: #333; }
        .stat { font-size: 1.2rem; margin-bottom: 1rem; color: #555; }
        .stat span { font-weight: bold; color: #00a884; }
        form { display: flex; flex-direction: column; margin-top: 2rem; }
        input { margin-bottom: 1rem; padding: 0.8rem; border: 1px solid #ddd; border-radius: 4px; }
        button { padding: 0.8rem; background: #00a884; color: white; border: none; border-radius: 4px; cursor: pointer; font-size: 1rem; }
        button:hover { background: #008f6f; }
        .logout { color: #d9534f; text-decoration: none; }
        .code-display { margin-top: 2rem; padding: 1.5rem; background: #e8f5e9; border: 1px solid #c8e6c9; border-radius: 4px; text-align: center; }
        .code-text { font-size: 2rem; letter-spacing: 5px; font-family: monospace; font-weight: bold; color: #333; margin: 1rem 0; }
    </style>
</head>
<body>
    <div class="header">
        <h2>Bienvenido, <%= username %></h2>
        <a href="/logout" class="logout">Cerrar Sesión</a>
    </div>

    <div class="card">
        <h1>Gestor de SubBots</h1>
        <div class="stat">SubBots Conectados: <span><%= connectedSubbots %></span></div>

        <form action="/request-code" method="POST">
            <label for="phoneNumber">Ingresa tu número (con código de país, sin +)</label>
            <input type="text" id="phoneNumber" name="phoneNumber" placeholder="Ej: 521234567890" required>
            <button type="submit">Pedir Código</button>
        </form>

        <% if (code) { %>
            <div class="code-display">
                <h3>Tu Código de Vinculación:</h3>
                <div class="code-text"><%= code %></div>
                <p>Copia este código y úsalo en WhatsApp > Dispositivos vinculados > Vincular con número de teléfono.</p>
            </div>
        <% } %>
    </div>
</body>
</html>
